interface User {
    id: String
    email: String
    firstName: String
    lastName: String
    tasks: [Task]
    groups: [Group]
}

type UserEntity implements User {
    id: String
    email: String
    firstName: String
    lastName: String
    tasks: [Task]
    groups: [Group]
}